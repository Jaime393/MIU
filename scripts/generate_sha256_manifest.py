Corregir `SHA256_maifest.txt` a `SHA256_manifest.txt` y regenerarlo exclusivamente desde bytes reales después de restaurar el código. Mejorar `scripts/generate_sha256_manifest.py` para salida determinista, exclusiones documentadas y verificación CI; prohibir placeholders y marcar explícitamente artefactos ausentes.