# Dependencias para el proyecto MIU
numpy>=1.24
scipy>=1.10
matplotlib>=3.7
pytest>=7.0
gudhi>=3.8   # opcional, para homología persistente (compute_betti)
# Nota: gudhi puede no estar disponible en Termux fácilmente. Si no, la función fallback será usada.