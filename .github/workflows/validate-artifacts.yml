Ampliar `.github/workflows/validate-artifacts.yml` para detectar contenido idéntico en múltiples fuentes, extensiones incompatibles con MIME/magic bytes, PDF bajo `.yml`, Markdown bajo `.py/.json`, CSV incrustado en código, manifiestos con placeholders y archivos stub. Ejecutar sobre PR y push; fallar con reporte de rutas y hashes. Añadir fixtures positivos y negativos.