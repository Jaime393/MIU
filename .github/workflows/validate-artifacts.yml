Completar la compuerta de integridad para detectar extensiones cuyo contenido real sea Markdown/PDF/CSV, archivos de código idénticos, stubs, manifests con placeholders y secretos. Ejecutarla en pull requests y en `main`; fallar con un informe accionable y conservar fixtures legítimos bajo `tests/fixtures/`. Integrar la validación existente de `.github/workflows/validate-artifacts.yml` en vez de crear otro workflow paralelo.